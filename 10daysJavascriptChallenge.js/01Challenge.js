/*
2–3 hours

Parse JSON data from a mock API response.

Convert an array of objects into different structures (e.g., group by category, sort by date).

Write a function to flatten nested objects.

Scenario: Build a utility that takes e-commerce product data and outputs reports like:

Products grouped by category

Cheapest product per category

Top 3 most expensive products
*/

